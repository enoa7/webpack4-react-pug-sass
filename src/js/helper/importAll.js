/**
 * @name importAll
 * @description import all files using cache
 * @see https://webpack.js.org/guides/dependency-management/#context-module-api
 */
export const importAll = r => {
  r.keys().forEach(r);
};
